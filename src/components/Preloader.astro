<!-- ---
import { useState, useEffect } from "react"
const [loading, setLoading] = Astro.useState(true)

// Hide loader after page loads
Astro.onMount(() => {
  window.addEventListener("load", () => {
    setTimeout(() => setLoading(false), 800) // delay for smoothness
  })
})
---

{loading && (
  <div class="fixed inset-0 flex items-center justify-center bg-black z-[9999]">
    <div class="text-center">
      <div class="w-16 h-16 border-4 border-white border-t-transparent rounded-full animate-spin mx-auto"></div>
      <p class="mt-4 text-white text-lg font-bold">Loading...</p>
    </div>
  </div>
)} -->
